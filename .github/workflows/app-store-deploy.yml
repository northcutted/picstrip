name: App Store Deploy
on:
  release:
    types:
      - published
  workflow_dispatch:
    inputs:
      release_tag:
        type: string
        required: true
        description: Immutable release tag; run this workflow on the same tag
      submit:
        type: boolean
        default: true
        description: Request production approval after staging
permissions:
  contents: read
concurrency:
  group: app-store-deploy-${{ github.ref }}
  cancel-in-progress: false
jobs:
  deploy:
    uses: northcutted/ios-release-workflows/.github/workflows/deploy.yml@71d5b6bb095d70500213a75d6c7f2365468bcffe
    with:
      config: .github/ios-release.json
      platform_revision: 71d5b6bb095d70500213a75d6c7f2365468bcffe
      release_tag: ${{ github.event.release.tag_name || inputs.release_tag }}
      submit: ${{ github.event_name == 'release' || inputs.submit }}
    permissions:
      contents: read
      actions: read
      id-token: write
      attestations: write
      artifact-metadata: write
