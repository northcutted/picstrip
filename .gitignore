# Xcode
#
# gitignore contributors: remember to update Global/Xcode.gitignore, Objective-C.gitignore & Swift.gitignore

## User settings
xcuserdata/

## Claude Code session state (locked agent worktrees, plans, settings)
.claude/

## Obj-C/Swift specific
*.hmap

## App packaging
*.ipa
*.dSYM.zip
*.dSYM

## Playgrounds
timeline.xctimeline
playground.xcworkspace

# Swift Package Manager
#
# Add this line if you want to avoid checking in source code from Swift Package Manager dependencies.
# Packages/
# Package.pins
# Package.resolved
# *.xcodeproj
#
# Xcode automatically generates this directory with a .xcworkspacedata file and xcuserdata
# hence it is not needed unless you have added a package configuration file to your project
# .swiftpm

.build/

# CocoaPods
#
# We recommend against adding the Pods directory to your .gitignore. However
# you should judge for yourself, the pros and cons are mentioned at:
# https://guides.cocoapods.org/using/using-cocoapods.html#should-i-check-the-pods-directory-into-source-control
#
# Pods/
#
# Add this line if you want to avoid checking in source code from the Xcode workspace
# *.xcworkspace

# Carthage
#
# Add this line if you want to avoid checking in source code from Carthage dependencies.
# Carthage/Checkouts

Carthage/Build/

# fastlane
fastlane/report.xml
fastlane/Preview.html
fastlane/test_output/
xcuserdata/
*.xccheckout
*.moved-aside
*.xcuserstate
*.xcscmblueprint
DerivedData/
build/

# Screenshots:
#   - Raw simulator captures stay ignored except en-US/ (used as a pipeline
#     cache). Other locales are reproducible via ``make screenshots``.
#   - frameit's ``*_framed.png`` variants are ignored everywhere — we draw
#     our own device frame in the Python compositor and don't ship frameit
#     output.
#   - ``processed/`` (final marketing PNGs) is tracked via Git LFS — that's
#     what ``screenshots.yml`` uploads to App Store Connect.
fastlane/screenshots/**/*.png
!fastlane/screenshots/en-US/*.png
!fastlane/screenshots/processed/**/*.png
fastlane/screenshots/en-US/*_framed.png
fastlane/screenshots/**/*.html

# Artifacts
*.ipa
*.dSYM
*.dSYM.zip
*.app.dSYM.zip

# Secrets / env
.env
.env.*
!.env.example
fastlane/api_key.json

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Node
node_modules/
npm-debug.log*

# Ruby
vendor/bundle/
.bundle/
